<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Angel Cloud Intelligence Hub</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap');

```
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Orbitron', monospace;
        background: #000;
        color: #00ffff;
        overflow-x: hidden;
        cursor: none;
    }

    /* Custom Cursor */
    .cursor {
        position: fixed;
        width: 20px;
        height: 20px;
        background: radial-gradient(circle, #00ffff 0%, transparent 70%);
        border-radius: 50%;
        pointer-events: none;
        z-index: 10000;
        mix-blend-mode: screen;
        transition: transform 0.1s ease;
    }

    /* CRT Screen Effect */
    .crt-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: 
            radial-gradient(ellipse at center, transparent 0%, rgba(0,0,0,0.3) 100%),
            linear-gradient(transparent 50%, rgba(0,255,255,0.03) 50%);
        background-size: 100% 100%, 100% 4px;
        pointer-events: none;
        z-index: 9999;
    }

    .crt-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent 0%, rgba(255,255,255,0.03) 50%, transparent 100%);
        animation: scanline 0.5s linear infinite;
    }

    @keyframes scanline {
        0% { transform: translateX(-100%); }
        100% { transform: translateX(100vw); }
    }

    /* Glitch Effects */
    .glitch {
        position: relative;
        animation: glitch-text 2s infinite;
    }

    .glitch::before,
    .glitch::after {
        content: attr(data-text);
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    .glitch::before {
        color: #ff0080;
        z-index: -1;
        animation: glitch-1 0.5s infinite;
    }

    .glitch::after {
        color: #00ff80;
        z-index: -2;
        animation: glitch-2 0.5s infinite;
    }

    @keyframes glitch-text {
        0%, 90%, 100% { transform: translate(0); }
        10% { transform: translate(-2px, 2px); }
        20% { transform: translate(2px, -2px); }
        30% { transform: translate(-2px, -2px); }
        40% { transform: translate(2px, 2px); }
        50% { transform: translate(-2px, 2px); }
        60% { transform: translate(2px, -2px); }
        70% { transform: translate(-2px, -2px); }
        80% { transform: translate(2px, 2px); }
    }

    @keyframes glitch-1 {
        0%, 100% { transform: translate(0); }
        10% { transform: translate(-5px, 2px); }
        20% { transform: translate(-10px, 1px); }
        30% { transform: translate(5px, -1px); }
        40% { transform: translate(-5px, 2px); }
        50% { transform: translate(-10px, 1px); }
        60% { transform: translate(5px, -1px); }
        70% { transform: translate(-5px, 1px); }
        80% { transform: translate(-10px, -1px); }
        90% { transform: translate(5px, 2px); }
    }

    @keyframes glitch-2 {
        0%, 100% { transform: translate(0); }
        10% { transform: translate(5px, -2px); }
        20% { transform: translate(10px, -1px); }
        30% { transform: translate(-5px, 1px); }
        40% { transform: translate(5px, -2px); }
        50% { transform: translate(10px, -1px); }
        60% { transform: translate(-5px, 1px); }
        70% { transform: translate(5px, -1px); }
        80% { transform: translate(10px, 1px); }
        90% { transform: translate(-5px, -2px); }
    }

    /* Boot Sequence */
    .boot-sequence {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: #000;
        z-index: 10000;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        transition: opacity 1s ease;
    }

    .boot-hidden {
        opacity: 0;
        pointer-events: none;
    }

    .boot-text {
        font-size: 24px;
        color: #00ff00;
        text-align: center;
        margin: 10px 0;
        opacity: 0;
        animation: fadeInUp 0.5s ease forwards;
    }

    .boot-text:nth-child(1) { animation-delay: 0.5s; }
    .boot-text:nth-child(2) { animation-delay: 1s; }
    .boot-text:nth-child(3) { animation-delay: 1.5s; }
    .boot-text:nth-child(4) { animation-delay: 2s; }
    .boot-text:nth-child(5) { animation-delay: 2.5s; }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .progress-bar {
        width: 60%;
        height: 4px;
        background: #333;
        margin: 30px 0;
        border-radius: 2px;
        overflow: hidden;
    }

    .progress-fill {
        height: 100%;
        background: linear-gradient(90deg, #00ff00, #00ffff);
        width: 0;
        animation: loadProgress 3s ease-in-out forwards;
        animation-delay: 1s;
    }

    @keyframes loadProgress {
        to { width: 100%; }
    }

    /* Main Interface */
    .main-interface {
        opacity: 0;
        transition: opacity 1s ease;
    }

    .interface-visible {
        opacity: 1;
    }

    /* Header */
    .header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 80px;
        background: linear-gradient(135deg, rgba(0,255,255,0.1), rgba(255,0,128,0.1));
        border-bottom: 2px solid #00ffff;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 30px;
        z-index: 1000;
        backdrop-filter: blur(10px);
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 15px;
    }

    .logo-icon {
        width: 50px;
        height: 50px;
        background: linear-gradient(45deg, #00ffff, #ff0080);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        font-weight: 900;
        color: #000;
        box-shadow: 0 0 20px rgba(0,255,255,0.5);
        animation: pulse 2s infinite;
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); box-shadow: 0 0 20px rgba(0,255,255,0.5); }
        50% { transform: scale(1.05); box-shadow: 0 0 30px rgba(0,255,255,0.8); }
    }

    .logo-text {
        font-size: 28px;
        font-weight: 900;
        background: linear-gradient(45deg, #00ffff, #ff0080);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .user-info {
        display: flex;
        align-items: center;
        gap: 20px;
    }

    .status-indicator {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 16px;
        background: rgba(0,255,255,0.1);
        border: 1px solid #00ffff;
        border-radius: 20px;
    }

    .status-dot {
        width: 8px;
        height: 8px;
        background: #00ff00;
        border-radius: 50%;
        animation: blink 1s infinite;
    }

    @keyframes blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0.3; }
    }

    /* Navigation */
    .nav-container {
        position: fixed;
        left: 0;
        top: 80px;
        width: 280px;
        height: calc(100vh - 80px);
        background: linear-gradient(180deg, rgba(0,0,0,0.9), rgba(0,20,40,0.9));
        border-right: 2px solid #00ffff;
        padding: 30px 0;
        z-index: 900;
    }

    .nav-item {
        display: flex;
        align-items: center;
        gap: 15px;
        padding: 15px 30px;
        margin: 5px 0;
        cursor: pointer;
        transition: all 0.3s ease;
        border-left: 4px solid transparent;
    }

    .nav-item:hover {
        background: rgba(0,255,255,0.1);
        border-left-color: #00ffff;
        transform: translateX(10px);
    }

    .nav-item.active {
        background: linear-gradient(90deg, rgba(0,255,255,0.2), transparent);
        border-left-color: #ff0080;
        color: #ff0080;
    }

    .nav-icon {
        width: 24px;
        height: 24px;
        fill: currentColor;
    }

    /* Main Content */
    .main-content {
        margin-left: 280px;
        margin-top: 80px;
        padding: 40px;
        min-height: calc(100vh - 80px);
        background: 
            radial-gradient(circle at 20% 30%, rgba(0,255,255,0.03) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(255,0,128,0.03) 0%, transparent 50%),
            linear-gradient(135deg, rgba(0,20,40,0.1), rgba(20,0,40,0.1));
    }

    .tab-content {
        display: none;
    }

    .tab-content.active {
        display: block;
        animation: fadeIn 0.5s ease;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* Stats Grid */
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 25px;
        margin-bottom: 40px;
    }

    .stat-card {
        background: linear-gradient(135deg, rgba(0,255,255,0.05), rgba(255,0,128,0.05));
        border: 1px solid rgba(0,255,255,0.3);
        border-radius: 15px;
        padding: 25px;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
    }

    .stat-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(0,255,255,0.1), transparent);
        transition: left 0.5s ease;
    }

    .stat-card:hover::before {
        left: 100%;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0,255,255,0.2);
        border-color: #00ffff;
    }

    .stat-value {
        font-size: 36px;
        font-weight: 900;
        background: linear-gradient(45deg, #00ffff, #ff0080);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 10px;
    }

    .stat-label {
        font-size: 14px;
        color: rgba(0,255,255,0.7);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    /* Project Cards */
    .projects-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 25px;
    }

    .project-card {
        background: linear-gradient(135deg, rgba(0,0,0,0.8), rgba(0,20,40,0.8));
        border: 1px solid rgba(0,255,255,0.3);
        border-radius: 15px;
        padding: 25px;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
    }

    .project-card::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #00ffff, #ff0080, #00ffff);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }

    .project-card:hover::after {
        transform: scaleX(1);
    }

    .project-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(0,255,255,0.2);
    }

    .project-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 15px;
    }

    .project-title {
        font-size: 20px;
        font-weight: 700;
        color: #00ffff;
        margin-bottom: 5px;
    }

    .project-type {
        font-size: 12px;
        color: rgba(0,255,255,0.6);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .project-status {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 5px 12px;
        border-radius: 15px;
        font-size: 12px;
        font-weight: 600;
    }

    .status-active {
        background: rgba(0,255,0,0.2);
        color: #00ff00;
        border: 1px solid #00ff00;
    }

    .status-development {
        background: rgba(255,255,0,0.2);
        color: #ffff00;
        border: 1px solid #ffff00;
    }

    .status-planning {
        background: rgba(255,0,128,0.2);
        color: #ff0080;
        border: 1px solid #ff0080;
    }

    /* Buttons */
    .btn {
        padding: 12px 24px;
        border: 2px solid #00ffff;
        background: transparent;
        color: #00ffff;
        font-family: 'Orbitron', monospace;
        font-weight: 600;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 1px;
        position: relative;
        overflow: hidden;
    }

    .btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(0,255,255,0.2), transparent);
        transition: left 0.5s ease;
    }

    .btn:hover::before {
        left: 100%;
    }

    .btn:hover {
        background: rgba(0,255,255,0.1);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0,255,255,0.3);
    }

    .btn-primary {
        border-color: #ff0080;
        color: #ff0080;
    }

    .btn-primary:hover {
        background: rgba(255,0,128,0.1);
        box-shadow: 0 5px 15px rgba(255,0,128,0.3);
    }

    /* Audio Visualizer */
    .audio-visualizer {
        position: fixed;
        bottom: 0;
        left: 280px;
        right: 0;
        height: 60px;
        background: linear-gradient(90deg, rgba(0,0,0,0.9), rgba(0,20,40,0.9));
        border-top: 1px solid rgba(0,255,255,0.3);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 2px;
        z-index: 800;
    }

    .visualizer-bar {
        width: 4px;
        background: linear-gradient(to top, #00ffff, #ff0080);
        border-radius: 2px;
        transition: height 0.1s ease;
    }

    /* Responsive */
    @media (max-width: 768px) {
        .nav-container {
            transform: translateX(-100%);
            transition: transform 0.3s ease;
        }
        
        .nav-container.mobile-open {
            transform: translateX(0);
        }
        
        .main-content {
            margin-left: 0;
            padding: 20px;
        }
        
        .audio-visualizer {
            left: 0;
        }
    }

    /* Terminal Window */
    .terminal {
        background: rgba(0,0,0,0.9);
        border: 1px solid #00ffff;
        border-radius: 8px;
        padding: 20px;
        font-family: 'Courier New', monospace;
        margin: 20px 0;
    }

    .terminal-header {
        display: flex;
        gap: 8px;
        margin-bottom: 15px;
    }

    .terminal-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
    }

    .dot-red { background: #ff5f56; }
    .dot-yellow { background: #ffbd2e; }
    .dot-green { background: #27ca3f; }

    .terminal-content {
        color: #00ff00;
        line-height: 1.4;
    }

    .command-line::before {
        content: '> ';
        color: #00ffff;
    }

    /* Panic Mode */
    .panic-mode {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: #000;
        z-index: 99999;
        display: none;
        justify-content: center;
        align-items: center;
        font-size: 48px;
        color: #00ff00;
    }

    /* Keyboard Shortcuts Help */
    .shortcuts-help {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: rgba(0,0,0,0.95);
        border: 2px solid #00ffff;
        border-radius: 15px;
        padding: 30px;
        z-index: 10000;
        display: none;
        max-width: 500px;
        width: 90%;
    }

    .shortcuts-grid {
        display: grid;
        grid-template-columns: 1fr 2fr;
        gap: 15px;
        align-items: center;
    }

    .shortcut-key {
        background: rgba(0,255,255,0.1);
        border: 1px solid #00ffff;
        border-radius: 5px;
        padding: 5px 10px;
        text-align: center;
        font-weight: 600;
    }
</style>
```

</head>
<body>
    <!-- Custom Cursor -->
    <div class="cursor"></div>

```
<!-- CRT Effect -->
<div class="crt-container"></div>

<!-- Boot Sequence -->
<div class="boot-sequence" id="bootSequence">
    <div class="boot-text">ANGEL CLOUD SYSTEMS</div>
    <div class="boot-text">INITIALIZING NEURAL NETWORKS...</div>
    <div class="boot-text">LOADING AI MODULES...</div>
    <div class="boot-text">ESTABLISHING SECURE CONNECTIONS...</div>
    <div class="boot-text glitch" data-text="WELCOME TO THE FUTURE">WELCOME TO THE FUTURE</div>
    <div class="progress-bar">
        <div class="progress-fill"></div>
    </div>
</div>

<!-- Panic Mode -->
<div class="panic-mode" id="panicMode">
    <div class="glitch" data-text="SYSTEM NORMAL">SYSTEM NORMAL</div>
</div>

<!-- Shortcuts Help -->
<div class="shortcuts-help" id="shortcutsHelp">
    <h3 style="margin-bottom: 20px; color: #00ffff;">Keyboard Shortcuts</h3>
    <div class="shortcuts-grid">
        <div class="shortcut-key">1-5</div>
        <div>Switch Tabs</div>
        <div class="shortcut-key">N</div>
        <div>New Project</div>
        <div class="shortcut-key">A</div>
        <div>New Agent</div>
        <div class="shortcut-key">Ctrl+P</div>
        <div>Panic Mode</div>
        <div class="shortcut-key">?</div>
        <div>Show This Help</div>
        <div class="shortcut-key">ESC</div>
        <div>Close Dialogs</div>
    </div>
    <button class="btn" onclick="toggleShortcuts()" style="margin-top: 20px; width: 100%;">Close</button>
</div>

<!-- Main Interface -->
<div class="main-interface" id="mainInterface">
    <!-- Header -->
    <header class="header">
        <div class="logo">
            <div class="logo-icon">⚡</div>
            <div class="logo-text glitch" data-text="ANGEL CLOUD">ANGEL CLOUD</div>
        </div>
        <div class="user-info">
            <div class="status-indicator">
                <div class="status-dot"></div>
                <span>SHANE - ADMIN</span>
            </div>
            <div class="status-indicator">
                <span id="systemTime"></span>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="nav-container">
        <div class="nav-item active" onclick="switchTab('overview')">
            <svg class="nav-icon" viewBox="0 0 24 24"><path d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"/></svg>
            <span>OVERVIEW</span>
        </div>
        <div class="nav-item" onclick="switchTab('projects')">
            <svg class="nav-icon" viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zM9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z"/></svg>
            <span>PROJECTS</span>
        </div>
        <div class="nav-item" onclick="switchTab('agents')">
            <svg class="nav-icon" viewBox="0 0 24 24"><path d="M20 9V7c0-1.1-.9-2-2-2h-3c0-1.66-1.34-3-3-3S9 3.34 9 5H6c-1.1 0-2 .9-2 2v2c-1.66 0-3 1.34-3 3s1.34 3 3 3v4c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2v-4c1.66 0 3-1.34 3-3s-1.34-3-3-3z"/></svg>
            <span>AI AGENTS</span>
        </div>
        <div class="nav-item" onclick="switchTab('users')">
            <svg class="nav-icon" viewBox="0 0 24 24"><path d="M16 7c0-2.21-1.79-4-4-4S8 4.79 8 7s1.79 4 4 4 4-1.79 4-4zM12 13c-2.67 0-8 1.34-8 4v3h16v-3c0-2.66-5.33-4-8-4z"/></svg>
            <span>USERS</span>
        </div>
        <div class="nav-item" onclick="switchTab('terminal')">
            <svg class="nav-icon" viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zM4 18V8h16v10H4z"/></svg>
            <span>TERMINAL</span>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Overview Tab -->
        <div class="tab-content active" id="overview">
            <h1 style="font-size: 48px; margin-bottom: 30px; background: linear-gradient(45deg, #00ffff, #ff0080); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">ANGEL CLOUD HUB</h1>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-value" id="totalUsers">24</div>
                    <div class="stat-label">Total Users</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="activeProjects">12</div>
                    <div class="stat-label">Active Projects</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="aiAgents">8</div>
                    <div class="stat-label">AI Agents Online</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value">99.9%</div>
                    <div class="stat-label">System Uptime</div>
                </div>
            </div>

            <div style="display: flex; gap: 20px; margin: 30px 0;">
                <button class="btn btn-primary" onclick="createProject()">NEW PROJECT</button>
                <button class="btn" onclick="createAgent()">NEW AI AGENT</button>
                <button class="btn" onclick="systemDiagnostics()">SYSTEM DIAGNOSTICS</button>
            </div>

            <div class="terminal">
                <div class="terminal-header">
                    <div class="terminal-dot dot-red"></div>
                    <div class="terminal-dot dot-yellow"></div>
                    <div class="terminal-dot dot-green"></div>
                </div>
                <div class="terminal-content">
                    <div class="command-line">angel-cloud --status</div>
                    <div>System Status: OPERATIONAL</div>
                    <div>Neural Networks: ACTIVE</div>
                    <div>Security Level: MAXIMUM</div>
                    <div>Last Update: <span id="lastUpdate"></span></div>
                </div>
            </div>
        </div>

        <!-- Projects Tab -->
        <div class="tab-content" id="projects">
            <h1 style="font-size: 36px; margin-bottom: 30px; color: #00ffff;">ACTIVE PROJECTS</h1>
            <div class="projects-grid" id="projectsGrid">
                <!-- Projects will be dynamically loaded -->
            </div>
        </div>

        <!-- AI Agents Tab -->
        <div class="tab-content" id="agents">
            <h1 style="font-size:
```
